# Πόρτα του API / static server
PORT=3000

# Μυστικό υπογραφής JWT — άλλαξέ το σε production
JWT_SECRET=change-me-in-production

# Πού αποθηκεύεται η SQLite βάση (default: ./data)
# DATA_DIR=/var/lib/mindbridge
